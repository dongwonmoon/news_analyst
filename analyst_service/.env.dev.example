# 로컬 개발 환경을 위한 환경 변수 예시 파일입니다.
# 이 파일의 복사본을 .env.dev 이름으로 만들고 실제 값을 채워주세요.

# .env.dev 파일은 .gitignore에 의해 버전 관리에서 제외되어야 합니다.

# ENV 변수는 config/config.{ENV}.yaml 파일을 로드하는 데 사용됩니다.
ENV=dev

# 로컬에서 테스트할 Kafka 브로커의 주소입니다.
# (예: EC2에 있는 Kafka 또는 로컬에 설치된 Kafka)
KAFKA_BOOTSTRAP_SERVERS=YOUR_KAFKA_BROKER_IP:29092

# 데이터베이스 연결 정보 (필요한 경우)
# DB_HOST=localhost
# DB_PORT=5432
# DB_USER=your_user
# DB_PASSWORD=your_password
# DB_NAME=your_db
